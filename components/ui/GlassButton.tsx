'use client';

import { motion, AnimatePresence } from 'framer-motion';

export default function GlassButton({
  label,
  onClick,
  disabled,
  active,
}: {
  label: string;
  onClick?: () => void;
  disabled?: boolean;
  active?: boolean;
}) {
  /* ⬇️ PASTE the entire `function GlassButton(...) { ... }` body here */
}
